name: Alunos-API - CI/CD

on:
  pull_request:
    branches: [ DEV, PR, main ]
    paths:
      - 'src/backend/alunos-api/**'

  push:
    branches: [ DEV, PR, main ]
    paths:
      - 'src/backend/alunos-api/**'

env:
  DOTNET_VERSION: '9.0.x'

jobs:

  # ===============================================
  # BUILD + TESTES DO MICROSSERVIÇO (CI RÁPIDO)
  # Rodado em:
  #   - PR → DEV, PR, main
  #   - Push → DEV, PR
  # ===============================================
  service-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Configuração .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore do microsserviço Alunos-API
        run: dotnet restore src/backend/alunos-api/Alunos.API.sln

      - name: Build do microsserviço Alunos-API
        run: dotnet build src/backend/alunos-api/Alunos.API.sln --configuration Release --no-restore

      - name: Execução de Testes Unitários Alunos-API
        run: dotnet test src/backend/tests/Alunos.Tests/Alunos.Tests.csproj --configuration Release

      - name: Execução de Testes Integrados Alunos-API
        run: dotnet test src/backend/tests/Alunos.IntegrationTests/Alunos.IntegrationTests.csproj --configuration Release

  # ===============================================
  # TESTES COMPLETOS (APENAS MAIN = CI completo)
  # ===============================================
  full-tests:
    needs: service-tests
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Configuração .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore Full da Solution MBA.Modulo5
        run: dotnet restore src/backend/MBA.Modulo5.sln

      - name: Execução de Testes do eco-sistema
        run: dotnet test src/backend/MBA.Modulo5.sln --configuration Release 

  # ===============================================
  # DEPLOY (DOCKER BUILD & PUSH)
  # Somente MAIN
  # ===============================================
  docker-build-push:
    needs: full-tests
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Credenciais do repositório Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Criação da imagem e Push no repositório do Docker Hub do microsserviço Alunos-API
        uses: docker/build-push-action@v5
        with:
          context: src/backend
          file: src/backend/alunos-api/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/project5-alunos-api:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/project5-alunos-api:${{ github.sha }}
