apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: plataforma-educacional
spec:
  selector:
    app: frontend
  type: NodePort  # facilita o acesso no minikube/kind
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
